// import { create } from "zustand";
// import { Map } from "@prisma/client";

// const useStore = create((set) => ({
//   maps: [] as Map[],
//   removeMap: (id: string) =>
//     set((state) => ({
//       maps: state.maps.filter((map) => map.id !== id),
//     })),
//   removeAllMaps: () => set({ maps: 0 }),
//   updateMaps: (newMaps) => set({ maps: newMaps }),
// }));
